# ============================================================
# .env.example — Copy this file and rename it to .env
# Fill in YOUR actual values below
# ============================================================

# ── PostgreSQL (users table / authentication) ──────────────
POSTGRES_URL=postgresql://USERNAME:PASSWORD@HOST/DATABASE

# ── MongoDB (screenshots / raw_samples) ───────────────────
MONGODB_URL=mongodb://USERNAME:PASSWORD@HOST:PORT/
MONGODB_DB_NAME=ai_assistant
